extends layout
append head
  title 专业师资
block member-bar
  li.active: a(href='#') 专业师资
block page
  .col-xs-9.col-xs-offset-1#member
    .alert.alert-info &nbsp;&nbsp;&nbsp;&nbsp;德艺美艺术教育中心的每位教师都来自音乐学院专业团体，有多年的教学经验。我校还会定期聘请来自北京及全国各地的名人、明显舞蹈家、演奏家和教授莅临我校指导，使学员们一睹明星风采。最主要的是得到最专业的学习培训，不走弯路。

    .box
      - member.forEach(function(m, i){
      - if(i%2==0){
      .row
        .col-xs-12.description
          .col-xs-3.center
            h4.text-center #{m.name}
            img.col-xs-12.pull-left(src='#{m.src}' alt)
          .col-xs-9
            - if(m.school){
            p.text-inverse #{m.school}
            - }
            - if(m.speciality){
            - m.speciality.split(",").forEach(function(d, j){
            p(style="margin-right:15px").label.label-success #{d}
            - })
            - }
            div(style="margin-top:12px")
              p.text-info 主要业绩
              ul
                - m.experience.split("\n").forEach(function(d, j){
                li.text-info #{d}
                - })
            p.text-danger #{m.description}
      - }else{
      .row
        .col-xs-12.description
          .col-xs-9
            - if(m.school){
            p.text-inverse #{m.school}
            - }
            - if(m.speciality){
            - m.speciality.split(",").forEach(function(d, j){
            p(style="margin-right:15px").label.label-success #{d}
            - })
            - }
            div(style="margin-top:12px")
              p.text-info 主要业绩
              ul
                - m.experience.split("\n").forEach(function(d, j){
                li.text-info #{d}
                - })
            p.text-danger #{m.description}
          .col-xs-3
            h4.text-center #{m.name}
            img.col-xs-12.pull-right(src='#{m.src}' alt)
      - }
      - })
